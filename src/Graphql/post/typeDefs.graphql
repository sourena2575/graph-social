type Post {
  id: ID
  userName: String
  body: String
  createdAt: String
  updatedAt: String
}
type Query {
  posts: [Post]
  post(postId: String!): Post
}
type Mutation {
  createPost(body: String!): Boolean
}
